import { UpperDirectiveDirective } from './upper-directive.directive';

describe('UpperDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UpperDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
